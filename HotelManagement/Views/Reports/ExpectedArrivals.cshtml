@model List<HotelManagement.Models.Reservation>
@using System.Globalization
@using System.Linq

@{
    ViewData["Title"] = "Oczekiwane przyjazdy";
    Layout = "_Layout";

    var selectedDate = (DateTime)ViewBag.SelectedDate;

    var totalReservations = Model?.Count ?? 0;
    var totalPersons = Model?.Sum(r => r.PersonCount) ?? 0;
}

<style>
    .arrivals-page {
        display: flex;
        flex-direction: column;
        gap: 1.25rem;
    }

    .arrivals-header {
        display: flex;
        justify-content: space-between;
        align-items: flex-start;
        gap: 1rem;
    }

    .arrivals-title-wrap {
        display: flex;
        flex-direction: column;
        gap: .35rem;
    }

    .arrivals-pill {
        display: inline-flex;
        align-items: center;
        gap: .35rem;
        padding: .18rem .6rem;
        border-radius: 999px;
        font-size: .72rem;
        text-transform: uppercase;
        letter-spacing: .12em;
        background: rgba(37, 99, 235, 0.08);
        border: 1px solid rgba(191, 219, 254, 0.9);
        color: #1d4ed8;
    }

        .arrivals-pill i {
            font-size: .9rem;
        }

    .arrivals-heading {
        display: flex;
        align-items: center;
        gap: .5rem;
        font-size: 1.25rem;
        font-weight: 600;
        margin: 0;
    }

        .arrivals-heading i {
            font-size: 1.4rem;
            color: #1d4ed8;
        }

    .arrivals-subtitle {
        font-size: .86rem;
        color: #6b7280;
        margin: 0;
    }

    .arrivals-meta {
        display: flex;
        flex-direction: column;
        align-items: flex-end;
        gap: .35rem;
        font-size: .8rem;
        color: #6b7280;
    }

    .arrivals-meta-badge {
        border-radius: 999px;
        padding: .2rem .7rem;
        border: 1px solid rgba(148, 163, 184, 0.9);
        background: #f9fafb;
        display: inline-flex;
        align-items: center;
        gap: .35rem;
    }

    .arrivals-summary-row {
        display: flex;
        flex-wrap: wrap;
        gap: .5rem;
        font-size: .8rem;
    }

    .arrivals-summary-pill {
        border-radius: .7rem;
        padding: .25rem .65rem;
        background: #f9fafb;
        border: 1px solid rgba(209, 213, 219, 0.9);
        display: inline-flex;
        align-items: center;
        gap: .35rem;
    }

    .arrivals-filters-card {
        border-radius: .9rem;
        border: 1px solid rgba(209, 213, 219, 0.9);
        background: #ffffff;
        box-shadow: 0 8px 20px rgba(15, 23, 42, 0.08);
        padding: .9rem 1rem;
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        justify-content: space-between;
        gap: .75rem;
    }

    .arrivals-filters-left {
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        gap: .6rem;
        font-size: .8rem;
    }

    .arrivals-filters-right {
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        gap: .5rem;
    }

    .arrivals-filters-form .form-control {
        font-size: .8rem;
        border-radius: .55rem;
        height: 32px;
        padding-inline: .5rem;
    }

    .arrivals-filters-form .btn {
        font-size: .8rem;
        border-radius: .7rem;
        height: 32px;
        display: inline-flex;
        align-items: center;
        gap: .3rem;
        padding-inline: .8rem;
    }

    .arrivals-table-wrapper {
        border-radius: .9rem;
        border: 1px solid rgba(209, 213, 219, 0.9);
        background: #ffffff;
        box-shadow: 0 10px 24px rgba(15, 23, 42, 0.08);
        overflow: hidden;
    }

    .arrivals-table-caption {
        padding: .6rem .95rem .4rem;
        font-size: .78rem;
        color: #6b7280;
        border-bottom: 1px solid rgba(229, 231, 235, 0.9);
    }

    .table-arrivals {
        font-size: .85rem;
        margin-bottom: 0;
    }

        .table-arrivals thead th {
            font-size: .75rem;
            text-transform: uppercase;
            letter-spacing: .08em;
            color: #6b7280;
            white-space: nowrap;
        }

        .table-arrivals tbody td {
            vertical-align: middle;
            padding-top: .55rem;
            padding-bottom: .55rem;
        }

    .arrivals-guest-name {
        font-weight: 600;
        font-size: .9rem;
    }

    .arrivals-guest-meta {
        font-size: .78rem;
        color: #6b7280;
    }

    .arrivals-room-pill {
        display: inline-flex;
        align-items: center;
        gap: .3rem;
        border-radius: 999px;
        padding: .15rem .55rem;
        background: #eff6ff;
        color: #1d4ed8;
        font-size: .78rem;
    }

    @@media (max-width: 992px) {
        .arrivals-header {
            flex-direction: column;
            align-items: flex-start;
        }

        .arrivals-meta {
            align-items: flex-start;
        }

        .arrivals-filters-card {
            align-items: flex-start;
        }

        .arrivals-filters-left,
        .arrivals-filters-right {
            width: 100%;
            justify-content: space-between;
        }
    }

    @@media (max-width: 768px) {
        .arrivals-filters-left,
        .arrivals-filters-right {
            flex-direction: column;
            align-items: flex-start;
        }

        .arrivals-heading {
            font-size: 1.05rem;
        }

        .table-arrivals {
            font-size: .8rem;
        }
    }
</style>

<div class="arrivals-page">
    <!-- HEADER -->
    <div class="arrivals-header">
        <div class="arrivals-title-wrap">
            <span class="arrivals-pill">
                <i class="bi bi-person-walking"></i>
                Raport operacyjny
            </span>
            <h2 class="arrivals-heading">
                <i class="bi bi-calendar2-check"></i>
                <span>Oczekiwane przyjazdy</span>
            </h2>
            <p class="arrivals-subtitle">
                Rezerwacje z datą <strong>Check-in</strong> na
                <strong>@selectedDate.ToString("dd MMMM yyyy", new CultureInfo("pl-PL"))</strong>.
            </p>
        </div>

        <div class="arrivals-meta">
            <div class="arrivals-meta-badge">
                <i class="bi bi-calendar-event"></i>
                Dzień raportu:
                <strong>@selectedDate.ToString("dd.MM.yyyy")</strong>
            </div>
            <div>
                <i class="bi bi-clock-history me-1"></i>
                Wygenerowano:
                <strong>@DateTime.Now.ToString("dd.MM.yyyy HH:mm:ss")</strong>
            </div>
        </div>
    </div>

    <!-- FILTRY + PDF -->
    <div class="arrivals-filters-card">
        <div class="arrivals-filters-left arrivals-filters-form">
            <form method="get"
                  asp-action="ExpectedArrivals"
                  class="d-flex flex-wrap align-items-center gap-2 mb-0">
                <div class="d-flex align-items-center gap-1">
                    <label for="date" class="small text-muted mb-0">Data przyjazdu:</label>
                    <input type="date"
                           id="date"
                           name="date"
                           value="@selectedDate.ToString("yyyy-MM-dd")"
                           class="form-control form-control-sm" />
                </div>
                <button type="submit" class="btn btn-outline-primary btn-sm">
                    <i class="bi bi-search"></i>
                    Pokaż
                </button>
            </form>
        </div>

        <div class="arrivals-filters-right">
            <div class="arrivals-summary-row">
                <div class="arrivals-summary-pill">
                    <i class="bi bi-people"></i>
                    <span>Rezerwacje: <strong>@totalReservations</strong></span>
                </div>
                <div class="arrivals-summary-pill">
                    <i class="bi bi-person-fill"></i>
                    <span>Osób łącznie: <strong>@totalPersons</strong></span>
                </div>
            </div>

            <form method="post"
                  asp-action="ExportExpectedArrivals"
                  class="m-0">
                @Html.AntiForgeryToken()
                <input type="hidden" name="date" value="@selectedDate.ToString("yyyy-MM-dd")" />
                <button type="submit" class="btn btn-outline-danger btn-sm">
                    <i class="bi bi-filetype-pdf me-1"></i>
                    Eksportuj do PDF
                </button>
            </form>
        </div>
    </div>

    <!-- TABELA -->
    @if (!Model.Any())
    {
        <div class="alert alert-info mb-0">
            <i class="bi bi-info-circle me-1"></i>
            Brak zaplanowanych przyjazdów w wybranym dniu.
        </div>
    }
    else
    {
        <div class="arrivals-table-wrapper">
            <div class="arrivals-table-caption">
                <i class="bi bi-list-ul me-1"></i>
                Lista oczekiwanych przyjazdów – @totalReservations rezerwacji, @totalPersons osób.
            </div>
            <div class="table-responsive">
                <table class="table table-hover table-arrivals align-middle text-center">
                    <thead class="table-light">
                        <tr>
                            <th>#</th>
                            <th>Gość</th>
                            <th>Pokój</th>
                            <th>Data przyjazdu</th>
                            <th>Data wyjazdu</th>
                            <th>Osób</th>
                        </tr>
                    </thead>
                    <tbody>
                        @{
                            int i = 1;
                            foreach (var r in Model)
                            {
                                <tr>
                                    <td>@i++</td>
                                    <td class="text-start">
                                        <div class="arrivals-guest-name">
                                            @($"{r.Guest.FirstName} {r.Guest.LastName}")
                                        </div>
                                        <div class="arrivals-guest-meta">
                                            @if (!string.IsNullOrWhiteSpace(r.Guest.Email))
                                            {
                                                <span>
                                                    <i class="bi bi-envelope me-1"></i>@r.Guest.Email
                                                </span>
                                            }
                                            @if (!string.IsNullOrWhiteSpace(r.Guest.Email) && !string.IsNullOrWhiteSpace(r.Guest.PhoneNumber))
                                            {
                                                <span class="mx-1">·</span>
                                            }
                                            @if (!string.IsNullOrWhiteSpace(r.Guest.PhoneNumber))
                                            {
                                                <span>
                                                    <i class="bi bi-telephone me-1"></i>@r.Guest.PhoneNumber
                                                </span>
                                            }
                                        </div>
                                    </td>
                                    <td>
                                        @if (r.Room != null && !string.IsNullOrWhiteSpace(r.Room.Number))
                                        {
                                            <span class="arrivals-room-pill">
                                                <i class="bi bi-door-closed"></i>
                                                Pokój @r.Room.Number
                                            </span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-light text-muted border">
                                                <i class="bi bi-door-closed me-1"></i>Brak przydziału
                                            </span>
                                        }
                                    </td>
                                    <td>@r.CheckIn.ToString("dd.MM.yyyy")</td>
                                    <td>@r.CheckOut.ToString("dd.MM.yyyy")</td>
                                    <td>@r.PersonCount</td>
                                </tr>
                            }
                        }
                    </tbody>
                </table>
            </div>
        </div>
    }
</div>
